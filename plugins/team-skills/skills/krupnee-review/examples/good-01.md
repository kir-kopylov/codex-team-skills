# Хороший Пример: Аудит Трёх Микрошагов

## Вход

Пользователь просит: "Проверь, правильно ли krupnee_lift сработал на переданном фрагменте диалога: переведи -> сократи -> сделай теплее."

## Ожидаемое Поведение

Codex фиксирует `trace_source: pasted_dialogue`, восстанавливает общий объект, считает `microstep_count`, оценивает scoring и показывает, что ожидаемый режим на третьем связанном шаге - `offer_lift`.

## Нельзя

Нельзя переписывать сам текст вместо аудита. Нельзя превращать review в runtime-подсказку пользователю.
