# Уже Идёт Локальный Зацикленный Разбор

## Вход

«Два свежих журнала показывают одинаковый timeout на одном маршруте. Мы опять получили тот же no-outcome. Закрой эту ветку и найди другой проверяемый поворот».

## Ожидаемое Поведение

Владелец задачи — `stuck-troubleshooting-reframe`: он фиксирует fingerprint и закрывает старую ветку. `kak-drugie-reshili` можно вызвать только вложенно для одного ограниченного внешнего вопроса.

## Нельзя

Нельзя выпускать ещё одну внешнюю карточку, игнорируя два локальных результата, или выдавать новый эксперимент за уже выполненный.
