---
name: ru-docx-render
description: >
  Нижний слой рендеринга документов в `.docx` по русскому стандарту оформления:
  `Times New Roman`, `A4`, поля 2/2/2.5/1.5 см, пустые графы «____», таблица
  подписей без рамок. Принимает готовую структуру (`JSON`-блоки или
  `markdown`-подмножество) и собирает файл через `python-docx`. Используйте, когда
  надо «свёрстать договор/расписку/акт в Word по стандарту», «отрендерить готовый
  текст в docx», «оформить документ как полагается — шрифт и поля». Skill не
  сочиняет содержание и не проверяет смысловые инварианты (суммы, хронологию,
  реквизиты) — это делает верхний skill; не заполняет пустые графы и не делает
  форматы кроме `.docx`.
---

# Ru Docx Render

## Запуск Навыка

Этот черновой навык запускайте только при явном вызове по имени или прямой команде пользователя. При одном смысловом совпадении не запускайте навык и не спрашивайте о его применении.

При явном вызове перед первым шагом покажите ровно одну короткую контекстную строку (не более 30 слов) и продолжайте работу в том же ответе, не ожидая реакции:

Применяю черновой навык **«Оформление документа Word по стандарту»** (обратная связь — @kir-kopylov): <кратко назовите конкретную пользу для текущего запроса>; продолжаю без ожидания.

Не включайте в строку `author_github`, внутреннее имя папки или пересказ всего запроса. Не спрашивайте, применять ли навык.

Если одновременно подходят совместимые навыки, выберите минимальный набор и покажите одну общую строку. Если подходы ведут к несовместимым результатам и запрос не позволяет выбрать, спросите только о желаемом результате, не о разрешении применить навык.

Запуск навыка не расширяет полномочия. Выполните всю безопасную и уже разрешённую часть; запросите подтверждение только непосредственно перед ещё не разрешённым внешним или изменяющим действием. Не запрашивайте повторно уже данное разрешение и не дублируйте системное окно подтверждения.

## Обзор

Это нижний, чисто технический слой пакета документов. Верхний skill (например, `remont-dogovor-i-raspiski` или `raspiska-o-poluchenii-deneg`) собирает смысл — текст, суммы, реквизиты, инварианты — и передаёт сюда уже готовую структуру. Задача `ru-docx-render` — превратить её в аккуратный `.docx` с единым оформлением, чтобы каждый верхний skill не переписывал вёрстку заново.

Стандарт оформления один на всех: шрифт `Times New Roman`, страница `A4`, поля верх/низ/лево/право = 2/2/2.5/1.5 см, заголовок по центру, тело по ширине, пустые графы личных данных остаются маркером «____», подписи — таблицей без видимых рамок.

## Естественные Входы

- «свёрстай договор/расписку в `docx` по стандарту»
- «отрендери готовый текст в `Word`»
- «собери `docx`: `Times New Roman`, поля, подписи»
- «сделай `Word`-файл из готовой структуры документа»
- «оформи документ как полагается — шрифт и поля»

## Процесс

1. **Примите готовую структуру.** Вход — либо `JSON` с непустым списком `blocks`, либо `markdown`-подмножество. Поддерживаемые блоки: `title`, `heading`, `para`, `city_date`, `signature_table`, `spacer`. Содержание уже собрано верхним слоем — не дополняйте и не правьте его по смыслу.
2. **Не заполняйте пустые графы.** Маркер «____» — это сознательно оставленное место для личных данных. Рендер переносит его как есть. Пустая графа в готовом `.docx` для этого skill — норма, а не дефект.
3. **Соберите файл.** Используйте `scripts/render_docx.py` и его хелперы: `new_doc` (страница, поля, базовый шрифт), `title`, `heading`, `para`, `city_date`, `signature_table`. Шрифт проставляется на каждый run (ascii + hAnsi + cs), чтобы кириллица не «съезжала» на дефолтный шрифт.
4. **Запуск через CLI.** `python3 scripts/render_docx.py --in <структура>.json --out <файл>.docx` (или `--in <текст>.md`). Выход обязан быть `.docx`; другой формат отклоняется.
5. **Верните путь к файлу.** Не делайте выводов о корректности содержания — за суммы, хронологию и реквизиты отвечает верхний skill.

## Границы

- Не сочинять содержание документа: текст, суммы и формулировки приходят сверху.
- Не проверять смысловые инварианты (итог = аванс + остаток, хронология, совпадение реквизитов) — это ответственность верхнего skill.
- Не заполнять пустые графы личных данных вместо человека: «____» остаётся «____».
- Не экспортировать в форматы кроме `.docx` (`PDF`, `ODT`, `HTML`).
- Не хранить и не вписывать приватные реквизиты в код или примеры: только синтетические данные.

## Опрос После Использования

Опрос задаётся один раз — после передачи готового .docx файла, не посреди рабочего цикла. Если пользователь уже ответил «пропустить» в этой сессии, не переспрашивайте.

```text
Опрос по skill:
1. Что в этом использовании ru-docx-render было полезно?
2. Что стоит доработать в skill или его формате?
Можно ответить коротко или написать "пропустить".
```

Если пользователь ответил, сохраните санированную карточку в `~/.codex/skill-runs/ru-docx-render/usage-feedback.jsonl` — лучше через bundled script:

```bash
python3 scripts/log_usage_feedback.py --liked "..." --improve "..." --outcome "..."
```

Script перед записью редактирует приватные пути, контакты и token-like строки и сохраняет `redaction_applied` и `redaction_types`. Если запись невозможна из-за sandbox, прав или отсутствия tools, не делайте вид, что лог сохранён: скажите об этом и покажите короткую JSONL-карточку для ручного сохранения. Raw-ответы, контакты, пути и секреты не коммитить.

## Логирование Сбоев

Перед выполнением прочитайте локальный `known-exceptions.yaml` как список уже известных случаев и применяйте подходящее `do_next_time` без нового поиска.

Если пользователь поправил skill, tool/API/browser упал, нарушен режим работы, пришлось искать workaround или skill сделал ложное предположение, запишите приватную карточку в `~/.codex/skill-runs/ru-docx-render/exception-log.jsonl`.

Пишите факты: что skill хотел сделать, что сделал, где сломался, какая предпосылка была ложной и что сделать в следующий раз. Если поле неизвестно, пишите `unknown`. Raw logs не коммитить.
